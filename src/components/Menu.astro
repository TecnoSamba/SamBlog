---
import "../styles/global.css";
---

<header>
	<div class="text">
		<a href="/"><h1>- {"<SamBlog />"} -</h1></a>
		<span>El blog de <strong>desarrollo web</strong> en Español</span>
	</div>
</header>

<style>
	header {
		display: flex;
		justify-content: center;
		align-items: center;
		margin: 10px;
		border-radius: 7px;
		gap: 2px;
		.text {
			display: flex;
			justify-content: center;
			flex-direction: column;
			align-items: center;
			h1 {
				font-family: "Fira Code";
			}
		}
		color: var(--font);
	}
	strong {
		background: linear-gradient(to right, #e976ef, #a053e4);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		border-radius: 4px;
	}
	a {
		text-decoration: none;
		color: var(--font);
		border-bottom: 0;
	}
	a:hover {
		border: 0;
	}
	a::before {
		height: 0;
	}
</style>
